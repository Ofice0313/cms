spring.application.name=cms
spring.profiles.active=${APP_PROFILE:dev}
spring.jpa.open-in-view=false

#
## DATASOURCE
#spring.datasource.url=jdbc:mysql://localhost:3306/cms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=@Marce6967
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Opcional: para ver as queries no console
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configurações do Spring MVC
spring.mvc.pathmatch.matching-strategy=ant_path_matcher


# Configurações do SpringDoc OpenAPI (Swagger)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
#springdoc.swagger-ui.enabled=true
#springdoc.swagger-ui.disable-swagger-default-url=true
#springdoc.show-actuator=true

# Security Jwt
spring.security.jwt.token.secret-key=53cr37
spring.security.jwt.token.expire-length=3600000

logging.level.org.springframework.security=TRACE
logging.level.org.springframework.security.authentication=DEBUG
logging.level.org.springframework.security.web=DEBUG
logging.level.com.mcts.cms.services.AuthService=DEBUG

